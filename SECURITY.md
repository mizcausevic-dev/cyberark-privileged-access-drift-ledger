# Security

This repository uses synthetic privileged-access evidence only.

Do not commit:

- CyberArk tenant URLs, vault exports, safe names, account names, or session recordings
- credentials, private keys, deploy tokens, or local machine paths
- real break-glass account identifiers or audit evidence packets

Before release, run local verification plus the standard sensitive-string and local-path scanner.
